# Example environment variables (do NOT commit real secrets here)

# Server
PORT=10000
NODE_ENV=development

# Primary connection (example placeholder)
# Replace with your real DATABASE_URL in production or in Render environment variables
DATABASE_URL=postgres://<USER>:<PASSWORD>@<HOST>:5432/<DATABASE>

# Individual DB parts (optional)
DB_HOST=localhost
DB_PORT=5432
DB_USER=alumno
DB_PASSWORD=123456
DB_NAME=course-db
DB_SSL=false

# Culqi / Payment provider (example)
CULQI_SECRET_KEY=
CULQI_API_URL=https://api.culqi.com
